# claude-

## MCP servers

This project configures the Unframer MCP server in `.mcp.json` via the
`UNFRAMER_MCP_URL` environment variable, which must be set to the full
server URL (including the `id` and `secret` query parameters) provided by
Unframer. Set this in your environment configuration — do not commit the
actual URL to this repository.